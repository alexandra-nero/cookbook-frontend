import React from 'react'
import Header from "./Header"

function CalorieLogApp({ setAccessToken }) {
  return (
      <Header styleValue = "blueMenuStyle"  setAccessToken={setAccessToken} />
  )
}

export default CalorieLogApp
